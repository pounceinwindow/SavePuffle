<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="GravityFalls.Client.Pages.CharacterSelectionPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Lobby">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Ink">#2B1B12</Color>
            <Color x:Key="Parchment1">#F6E7C6</Color>
            <Color x:Key="Parchment2">#EBD5A6</Color>
            <Color x:Key="Line">#B8935E</Color>

            <Color x:Key="Accent">#C85B23</Color>
            <Color x:Key="Muted">#7A5E3C</Color>
            <Color x:Key="Good">#2E7D32</Color>
            <Color x:Key="Warn">#B71C1C</Color>

            <LinearGradientBrush x:Key="ParchmentBrush" EndPoint="0,1">
                <GradientStop Color="{StaticResource Parchment1}" Offset="0" />
                <GradientStop Color="{StaticResource Parchment2}" Offset="1" />
            </LinearGradientBrush>

            <Style TargetType="Label" x:Key="H1">
                <Setter Property="TextColor" Value="{StaticResource Ink}" />
                <Setter Property="FontSize" Value="26" />
                <Setter Property="FontAttributes" Value="Bold" />
            </Style>

            <Style TargetType="Label" x:Key="H2">
                <Setter Property="TextColor" Value="{StaticResource Ink}" />
                <Setter Property="FontSize" Value="18" />
                <Setter Property="FontAttributes" Value="Bold" />
            </Style>

            <Style TargetType="Label" x:Key="SmallMuted">
                <Setter Property="TextColor" Value="{StaticResource Muted}" />
                <Setter Property="FontSize" Value="12" />
            </Style>

            <Style TargetType="Button" x:Key="PrimaryButton">
                <Setter Property="BackgroundColor" Value="{StaticResource Accent}" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="CornerRadius" Value="16" />
                <Setter Property="HeightRequest" Value="52" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="FontSize" Value="18" />
            </Style>

            <Style TargetType="Button" x:Key="ChipButton">
                <Setter Property="BackgroundColor" Value="#E8C98A" />
                <Setter Property="TextColor" Value="{StaticResource Ink}" />
                <Setter Property="CornerRadius" Value="14" />
                <Setter Property="HeightRequest" Value="40" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="Padding" Value="14,0" />
            </Style>

            <Style TargetType="Button" x:Key="HeroButton">
                <Setter Property="BackgroundColor" Value="#F9EFD6" />
                <Setter Property="TextColor" Value="{StaticResource Ink}" />
                <Setter Property="CornerRadius" Value="18" />
                <Setter Property="HeightRequest" Value="44" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="Padding" Value="14,0" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <!-- Background -->
        <Image Source="wood_bg.png" Aspect="AspectFill" />
        <BoxView Opacity="0.18" BackgroundColor="Black" />

        <ScrollView>
            <VerticalStackLayout Padding="24" Spacing="14"
                                 HorizontalOptions="Center"
                                 MaximumWidthRequest="900">

                <!-- Header -->
                <Border Stroke="{StaticResource Line}"
                        StrokeThickness="3"
                        StrokeShape="RoundRectangle 18"
                        Background="{StaticResource ParchmentBrush}">
                    <Border.Shadow>
                        <Shadow Opacity="0.45" Radius="18" Offset="0,8"/>
                    </Border.Shadow>

                    <Grid Padding="16" ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto" RowSpacing="6">
                        <Label Grid.Row="0" Grid.Column="0"
                               Text="ТАИНСТВЕННОЕ ЛОББИ"
                               Style="{StaticResource H1}" />

                        <Border Grid.Row="1" Grid.Column="0"
                                Stroke="{StaticResource Line}"
                                StrokeThickness="2"
                                StrokeShape="RoundRectangle 14"
                                BackgroundColor="#E8C98A"
                                Padding="12,6">
                            <HorizontalStackLayout Spacing="10">
                                <Label Text="👥 4 игрока" TextColor="{StaticResource Ink}" FontAttributes="Bold"/>
                                <Label Text="•" TextColor="{StaticResource Ink}" Opacity="0.6"/>
                                <Label Text="Выбор героев + баффы" TextColor="{StaticResource Ink}" FontAttributes="Bold"/>
                            </HorizontalStackLayout>
                        </Border>

                        <Button x:Name="BackButton" StyleClass="chipBtn"
                                Grid.Row="0" Grid.RowSpan="2" Grid.Column="1"
                                Text="Назад"
                                Style="{StaticResource ChipButton}"
                                HorizontalOptions="End"
                                VerticalOptions="Center" />
                    </Grid>
                </Border>

                <!-- Hero selection -->
                <Border Stroke="{StaticResource Line}"
                        StrokeThickness="3"
                        StrokeShape="RoundRectangle 18"
                        Background="{StaticResource ParchmentBrush}">
                    <Border.Shadow>
                        <Shadow Opacity="0.35" Radius="16" Offset="0,6"/>
                    </Border.Shadow>

                    <VerticalStackLayout Padding="14" Spacing="10">
                        <Label Text="Выберите персонажа" Style="{StaticResource H2}" />

                        <Grid ColumnDefinitions="*,*,*,*,*" ColumnSpacing="8">
                            <Button x:Name="HeroDipper" Text="🧢 Диппер" Style="{StaticResource HeroButton}" />
                            <Button x:Name="HeroMabel" Grid.Column="1" Text="🎀 Мэйбл" Style="{StaticResource HeroButton}" />
                            <Button x:Name="HeroStan" Grid.Column="2" Text="💼 Стэн" Style="{StaticResource HeroButton}" />
                            <Button x:Name="HeroSoos" Grid.Column="3" Text="🕶️ Зус" Style="{StaticResource HeroButton}" />
                            <Button x:Name="HeroWendy" Grid.Column="4" Text="🪓 Венди" Style="{StaticResource HeroButton}" />
                        </Grid>

                        <Border Stroke="{StaticResource Line}" StrokeThickness="2" StrokeShape="RoundRectangle 14" BackgroundColor="#F9EFD6" Padding="12,10">
                            <Label x:Name="HeroHint" Text="Выбран: ..." Style="{StaticResource SmallMuted}" />
                        </Border>
                    </VerticalStackLayout>
                </Border>

                <Label Text="Игроки" FontSize="20" FontAttributes="Bold" TextColor="White" Margin="0,6,0,0" />

                <CollectionView x:Name="CarouselView">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Stroke="{StaticResource Line}"
                                    StrokeThickness="3"
                                    StrokeShape="RoundRectangle 18"
                                    Background="{StaticResource ParchmentBrush}"
                                    Margin="0,6,0,0">
                                <Border.Shadow>
                                    <Shadow Opacity="0.25" Radius="12" Offset="0,5"/>
                                </Border.Shadow>

                                <Grid Padding="14"
                                      ColumnDefinitions="Auto,12,*,Auto"
                                      RowDefinitions="Auto,Auto"
                                      RowSpacing="6">

                                    <!-- Hero icon -->
                                    <Border Grid.RowSpan="2"
                                            Stroke="{StaticResource Line}"
                                            StrokeThickness="2"
                                            StrokeShape="RoundRectangle 999"
                                            BackgroundColor="#F9EFD6"
                                            HeightRequest="52"
                                            WidthRequest="52"
                                            VerticalOptions="Center">
                                        <Label Text="{Binding HeroEmoji}"
                                               FontSize="24"
                                               TextColor="{StaticResource Ink}"
                                               HorizontalOptions="Center"
                                               VerticalOptions="Center"/>
                                    </Border>

                                    <Label Grid.Column="2"
                                           Grid.Row="0"
                                           Text="{Binding Title}"
                                           TextColor="{StaticResource Ink}"
                                           FontAttributes="Bold"
                                           FontSize="16"
                                           LineBreakMode="TailTruncation"/>

                                    <HorizontalStackLayout Grid.Column="2" Grid.Row="1" Spacing="8">
                                        <BoxView WidthRequest="10" HeightRequest="10" CornerRadius="5"
                                                 BackgroundColor="{Binding StatusDotColor}"
                                                 VerticalOptions="Center"/>
                                        <Label Text="{Binding StatusText}" Style="{StaticResource SmallMuted}" />
                                        <Label Text="{Binding MeTag}" Style="{StaticResource SmallMuted}" />
                                    </HorizontalStackLayout>

                                    <Label Grid.Column="3" Grid.RowSpan="2" Text="{Binding ReadyChip}" TextColor="{StaticResource Ink}" FontAttributes="Bold" VerticalOptions="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12" Margin="0,10,0,8">
                    <Border Stroke="{StaticResource Line}"
                            StrokeThickness="2"
                            StrokeShape="RoundRectangle 16"
                            BackgroundColor="#E8C98A"
                            Padding="14,10">
                        <Label x:Name="LobbyHint"
                               Text="Нажми READY. Игра стартует когда все 4 готовы."
                               TextColor="{StaticResource Ink}"
                               FontAttributes="Bold"/>
                    </Border>

                    <Button x:Name="ConfirmButton" StyleClass="primaryBtn"
                            Grid.Column="1"
                            Text="READY"
                            Style="{StaticResource PrimaryButton}">
                        <Button.Shadow>
                            <Shadow Opacity="0.35" Radius="12" Offset="0,6" />
                        </Button.Shadow>
                    </Button>
                </Grid>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
